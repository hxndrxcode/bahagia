@layout('layout')

@section('content')
<div class="d-grid gap-3 mb-5">
  @each(c in categories)
  <a href="/store/{{c.slug}}" class="btn btn-outline-secondary d-flex">
    <i class="bi bi-{{c.icon}} my-1"></i>
    <span class="ms-3 mt-1 me-auto">{{c.name}}</span>
    <i class="bi bi-chevron-right my-1"></i>
  </a>
  @endeach
</div>
<h2 class="h4 mb-4 pb-2 border-bottom">Produk Disematkan</h2>
<div>

</div>
@endsection
